#include<stdio.h>
int main()
{
	int n,l,s,p,i,sum[i];
	char x[9];
	char c[9];
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%s",x);
		scanf("%d%d%d",&l,&s,&p);
		sum[i]=l+s+p;
		if(sum[i]>sum[i++] && i++!=n)
		{
			c[9]=x;
		}
	}
	printf("%s",c);
}
